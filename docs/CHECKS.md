# Automated Checks

Run the following command to ensure all code quality checks and tests pass after making changes to the codebase:

```bash
npm run validate
```

_Note: You can also use `npm run format` and `npm run lint:fix` to automatically fix formatting and linting issues before running these checks._
