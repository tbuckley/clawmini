# Subagent Policy — Developer Notes

This file is a shared scratchpad for developers implementing the
unified-delegation feature defined in `spec.html` and broken into
tickets in `tickets.md`. Add anything that another developer would
benefit from knowing: surprising file locations, gotchas, conventions,
patterns that worked, etc.

## Notes

### Repository layout

- **`src/shared/`** — types and config helpers reachable from both
  daemon and CLI. Zod schemas live here too (`config.ts`,
  `policies.ts`, now also `delegations.ts`).
- **`src/daemon/`** — daemon-only services: stores, routers, the
  agent loop. Singletons are exported at module scope at the bottom
  of the file that defines the class:
  - `src/daemon/agent/task-scheduler.ts` ends with
    `export const taskScheduler = new TaskScheduler();`
  - The future `DelegationManager` (Ticket 1) should follow the same
    pattern: `export const delegationManager = new DelegationManager();`.
- **`src/daemon/api/`** — tRPC routers (`agent-router.ts`,
  `subagent-router.ts`, `agent-policy-endpoints.ts`, etc.).
- **`src/cli/`** — Commander-based CLI commands. The new
  `delegations` group in Ticket 6 belongs here
  (`src/cli/delegations-commands.ts`).

### Zod conventions

- Import is `import { z } from 'zod';` (Zod 4.3 — see `package.json`).
- Existing code uses `z.looseObject({...})` for some on-disk schemas
  (see `src/shared/config.ts`); strict `z.object({...})` is fine too.
  `delegations.ts` uses strict objects because the shape is fully
  owned by us.
- Discriminated unions: `z.discriminatedUnion('kind', [...])` works
  in Zod 4, but **its inferred type can differ from a hand-written
  TS interface under `exactOptionalPropertyTypes: true`** — Zod
  infers `cwd: string | undefined`, the interface declares `cwd?: string`.
  The fix used in `delegation-store.ts` is the same idiom as
  `request-store.ts`: cast at the load boundary
  (`return parsed.data as Delegation`).

### ID generation idiom (carried over from `request-store.ts`)

- Today's `RequestStore` uses 3-char uppercase alphanumeric ids,
  generated by `generateRandomAlphaNumericString(3)`. The caller
  (in `PolicyRequestService.createRequest`) wraps it in a
  `do { … } while (collide)` loop and bumps to a longer id only if
  the loop spins forever (it doesn't — it just keeps rolling).
- `DelegationStore.generateId` keeps the same shape but **uses
  lowercase `[0-9a-z]`** (per spec §5.6: "3-char alphanum, unique
  per chat"). It also handles growth-on-saturation internally so
  callers don't have to. The default `attemptsBeforeGrow(length) =
  36 ** length` means: in practice you'll never grow past length 3
  for a single chat — but if a chat somehow saturated, we'd move to
  4 chars rather than spin forever.
- For testability `generateId` accepts an `opts` object with
  `randomFn` and `attemptsBeforeGrow` hooks; the test in
  `delegation-store.test.ts` uses both to verify growth deterministically.

### File-IO conventions

- All on-disk paths derive from `getClawminiDir(startDir)` in
  `src/shared/workspace.ts` (returns `<workspaceRoot>/.clawmini`).
  Tests mock that function — see `src/daemon/request-store.test.ts`
  and the new `src/daemon/delegation-store.test.ts`.
- Atomic writes: `src/shared/workspace.ts` has a `writeJsonFile`
  helper that writes to a tmp file and renames. The delegation
  store doesn't use it because delegations are written serially
  per id and a torn read is fine (next caller is the same daemon
  process). If Ticket 1+ adds concurrent writers, switch to the
  atomic helper.
- ENOENT detection: copy the local `isENOENT(err)` helper — there's
  no shared one yet.
- Layout: `<root>/.clawmini/tmp/delegations/<chatId>/<id>.json` and
  `<root>/.clawmini/tmp/delegations/<chatId>/subscriptions/<subscriptionId>.json`.

### Tests

- Unit tests sit next to the source as `*.test.ts` (vitest picks them
  up via `src/**/*.{test,spec}.{js,ts}` in `vitest.config.ts`).
- E2E tests live under `e2e/` and have their own global setup
  (`e2e/_helpers/global-setup.ts`).
- The command bar is `npm run validate` (format + lint + typecheck +
  full vitest run, including e2e). It also builds the web app — that
  takes ~30s and is unavoidable.

### Spec reading order

- `spec.html` §5.6 is the source of truth for the unified record /
  layout.
- §3.1 covers the state machine, §3.2 the two delivery modes, §3.3
  the defaults that preserve today's behavior.
- §4 covers approval rules (Ticket 4); §6 covers wait/subscribe
  semantics (Ticket 5).

### Ticket 0 status (done)

- `src/shared/delegations.ts` — types + `DelegationSchema`
  (discriminated union on `kind`) + `DelegationSubscription` +
  schema.
- `src/daemon/delegation-store.ts` — file-IO only; `save`, `load`,
  `list`, `delete`, `generateId`, subscription IO, `wipeAll`.
- `src/daemon/delegation-store.test.ts` — 17 unit tests.
- Nothing wired into RPCs yet. `RequestStore` and
  `ChatSettings.subagents` remain authoritative until Tickets 2/3.
