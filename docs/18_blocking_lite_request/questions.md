# Questions

## Question 1: Timeout behavior
Q: Should the blocking request command wait indefinitely for an approval/rejection (relying on the user to use Ctrl+C to cancel if needed), or should there be a built-in timeout (e.g., 5 minutes)?
A: It should wait indefinitely.
